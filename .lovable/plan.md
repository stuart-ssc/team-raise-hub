

## Goal
Update breadcrumbs on `/dashboard/groups` and `/dashboard/users` to reflect their new home under Settings.

## Investigation needed
Confirm current breadcrumb setup on both pages and how `DashboardPageLayout` `segments` prop is used elsewhere.
