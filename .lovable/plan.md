

## Goal
Add a "Preview" button alongside "Publish Campaign" and "Open in Editor" on the AI Campaign Builder completion screen, opening the campaign landing page (`/c/{slug}`) in a new tab.

## Investigation
Need to confirm where these two buttons live and whether the campaign `slug` is available at that point.
